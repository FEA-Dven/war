export const GRID_WIDTH = 40;
export const GRID_HEIGHT = 40;
export const GRID_MARGGIN = 10;
export const screenWidth = window.wx ? window.innerWidth : window.screen.width;
export const screenHeight = window.wx ? window.innerHeight : window.screen.height;
export const COLOR_ENUM = {
  WHITE: 'white',
  RED: 'red',
  BLUE: '#0096FF',
  GREEN: 'green'
}
export const BUTTON_WIDTH = screenWidth - 120;
export const BUTTON_HEIGHT = (screenWidth - 120) / 2;